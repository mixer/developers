=version 1
=resource User
=method get
=endpoint /users/:id/preferences
=description Retrieves preferences for the given user.

include ../../_mixins.jade

p This endpoint lists preferences for a user.

h3 Parameters

table.endpoint-param-table
    tr
        td.name :id
        td.description The numeric user ID.

h3 Possible Results

+result("Successful", "200 OK", "Note that if the user does not exist, we just return the default preferences. This endpoint does not give 404 responses.").
    {
        "chat:colors": true,
        "chat:emotes": true,
        "chat:sounds:notification": "ping",
        "chat:sounds:play": true,
        "chat:sounds:volume": 1,
        "chat:tagging": true,
        "chat:timestamps": false
    }
