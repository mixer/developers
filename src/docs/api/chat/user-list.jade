=version 1
=resource Chat
=method get
=endpoint /chats/:id/users
=description Displays a list of online users in the chat.

include ../../_mixins.jade

p Displays a list of online users in the chat, sorted in descending order by role.

h3 Parameters

table.endpoint-param-table
    tr
        td.name :id
        td.description The channel numeric id of the channel whose chat you want list.
    tr
        td.name page #[span optional]
        td.description
            | Page of results to get.
            .default 0
    tr
        td.name limit #[span optional]
        td.description
            | Number of results per page to retrieve.
            .default 50

h3 Possible Results

+result("Successful", "200 OK").
    [
        {
            "user_id": 1,
            "user_name": "Connor4312",
            "user_role": "Owner"
        },
        {
            "user_id": 2,
            "user_name": "ConMan12",
            "user_role": "Mod"
        }
    ]
