=version 1
=resource Chat
=method get
=endpoint /chats/:id/users/search
=description Searches for online users in a chat.

include ../../_mixins.jade

p Searches for online users by a matching substring in their username.

h3 Parameters

table.endpoint-param-table
    tr
        td.name :id
        td.description The channel numeric id of the channel whose chat you want search.
    tr
        td.name username
        td.description The username to search by.
    tr
        td.name page #[span optional]
        td.description
            | Page of results to get.
            .default 0
    tr
        td.name limit #[span optional]
        td.description
            | Number of results per page to retrieve.
            .default 50

h3 Possible Results

+result("Successful", "200 OK").
    [
        {
            "user_id": 1,
            "user_name": "Connor4312",
            "user_roles": ["Owner"]
        },
        {
            "user_id": 2,
            "user_name": "ConMan12",
            "user_roles": ["User", "Mod"]
        }
    ]
